/// Motion tokens. Decorative looping animation is not used.
abstract final class AppMotion {
  static const Duration short = Duration(milliseconds: 200);
  static const Duration medium = Duration(milliseconds: 240);
}
